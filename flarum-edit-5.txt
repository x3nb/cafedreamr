/* ============================================================
   1. GLOBAL ATMOSPHERE & FONTS
   ============================================================ */
@import url('https://fonts.googleapis.com/css2?family=Vampiro+One&family=Klee+One:wght@400;600&display=swap');

html, body {
  background: linear-gradient(180deg, #301934 0%, #000000 100%) no-repeat fixed !important;
  color: #D1B3FF !important;
  font-family: 'Klee One', sans-serif;
}

/* ============================================================
   2. HEADER & NAVIGATION (White Space Fix)
   ============================================================ */
.App-header, .Header-title, .Header-controls, .App-navigation {
  background: transparent !important;
  box-shadow: none !important;
  border: none !important;
}

.App-navigation {
  background: #000000 !important;
  border-bottom: 1px solid #bc13fe !important;
}

/* Status Bar Centering */
.grid-status-bar {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 20px;
  background: #000;
  padding: 8px 0;
  font-family: monospace;
  font-size: 11px;
}

.status-window {
  border: 1px solid rgba(188, 19, 254, 0.4);
  padding: 4px 12px;
  background: rgba(48, 25, 52, 0.6);
  color: #bc13fe;
}

/* Cyan Glowing Controls (Bell, Search) */
.Header-controls .Button--link, 
.Header-controls .Button--flat {
  color: #00ffff !important;
  text-shadow: 0 0 8px #00ffff;
}

/* ============================================================
   3. NEON TITLES & TYPING ANIMATION
   ============================================================ */
h1, h2, h3, .DiscussionListItem-title {
  font-family: 'Vampiro One', cursive !important;
  color: #bc13fe !important;
  text-shadow: 0 0 10px rgba(188, 19, 254, 0.7);
}

/* The Decryption Animation for Archive Titles */
.Hero-title {
  display: inline-block !important;
  white-space: nowrap;
  overflow: hidden;
  border-right: 3px solid #00ffff;
  width: 0;
  animation: 
    grid-type 2.5s steps(30, end) forwards,
    grid-blink .75s step-end infinite;
}

@keyframes grid-type {
  from { width: 0; }
  to { width: 100%; }
}

@keyframes grid-blink {
  from, to { border-color: transparent; }
  50% { border-color: #00ffff; }
}

/* ============================================================
   4. MEMBER WINDOWS & CARDS
   ============================================================ */
.UserCard {
  background: rgba(48, 25, 52, 0.7) !important;
  border: 1px solid #bc13fe !important;
  box-shadow: 0 0 15px rgba(188, 19, 254, 0.3);
  backdrop-filter: blur(10px);
  transition: all 0.4s ease;
}

.UserCard:hover {
  border-color: #00ffff !important;
  box-shadow: 0 0 25px rgba(0, 255, 255, 0.5);
  transform: scale(1.02);
}

/* ============================================================
   5. DISCUSSION LIST & POST CONTENT
   ============================================================ */
.DiscussionListItem {
  background: rgba(0, 0, 0, 0.3) !important;
  border-left: 3px solid #bc13fe !important;
  margin-bottom: 5px;
}

.DiscussionListItem:hover {
  border-left: 3px solid #00ffff !important;
}

.Post-body {
  font-size: 18px !important;
  color: #D1B3FF !important;
  line-height: 1.6;
}
/* --- Guest Welcome Box Image Overlay --- */
.fof-welcome-box-guest {
  background-size: cover !important;
  background-position: center !important;
  position: relative;
  z-index: 1;
}

/* Adds a dark tint so the text stays sharp against the image */
.fof-welcome-box-guest::before {
  content: "";
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0, 0, 0, 0.5); /* 50% darkness tint */
  z-index: -1;
  border-radius: 10px;
}
/* Styling the Alert Box that appears between posts */
.Alert.Alert--discussion {
  background: rgba(0, 123, 167, 0.1) !important; /* Faint Cerulean */
  border: 1px solid #00ffff !important;
  color: #00ffff !important;
  font-family: monospace;
  text-align: center;
  margin: 20px 0;
  box-shadow: 0 0 15px rgba(0, 255, 255, 0.2);
  padding: 15px !important;
}

.Alert.Alert--discussion strong {
  color: #bc13fe !important; /* Purple for the title */
  text-transform: uppercase;
}
/* --- User Directory: System Integration --- */

/* 1. Force the page background to match the Grid */
.UserDirectoryList, .UserDirectoryPage {
  background: transparent !important;
}

/* 2. Fix the White Background on the Directory Container */
.UserDirectoryList-content {
  background: rgba(20, 10, 25, 0.9) !important; /* Deep dark purple/black */
  border: 1px solid #bc13fe !important;
  box-shadow: 0 0 20px rgba(188, 19, 254, 0.2);
  border-radius: 10px;
  padding: 20px;
}

/* 3. Style the Search and Filter bars in the Directory */
.UserDirectory-search input, .UserDirectory-filter .Select-input {
  background: rgba(48, 25, 52, 0.6) !important;
  border: 1px solid #00ffff !important;
  color: #D1B3FF !important;
  text-shadow: 0 0 5px rgba(0, 255, 255, 0.5);
}

/* 4. Fix the individual User Cards in the Grid view */
.UserCard--directory {
  background: rgba(0, 0, 0, 0.5) !important;
  border: 1px solid rgba(188, 19, 254, 0.4) !important;
  transition: all 0.3s ease;
}

.UserCard--directory:hover {
  border-color: #00ffff !important;
  box-shadow: 0 0 15px #00ffff;
  transform: translateY(-5px);
}

/* 5. Fix any stubborn white text/headers */
.UserDirectory-title {
  color: #bc13fe !important;
  font-family: 'Vampiro One', cursive !important;
  text-shadow: 0 0 10px #bc13fe;
}
